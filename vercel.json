{
    "version": 2,
    "buildCommand": "composer install && php artisan config:clear && php artisan route:clear && php artisan view:clear",
    "env": {
        "APP_ENV": "production",
        "APP_DEBUG": "false"
    },
    "rewrites": [
        { "source": "/(.*)", "destination": "/api/index.php" }
    ]
}